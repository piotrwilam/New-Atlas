# @package _global_
# Paper Figure 10: circuit size by layer — Rust × Qwen, coloured by flow type.
# 75 concepts: 2 two_phase (Use, Super — the module-system analogues of
# Python's atomicity cluster), 71 late_emergence, 2 unclassified.

defaults:
  - /default
  - _self_

figure:
  name: figure10_circuit_size_rust_qwen

model: QW
lang: R
partition: universal

figsize: [14, 5]
style: paper
